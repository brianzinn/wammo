# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint nyc coverage output
coverage
# re-enable this to have the test directory linted (lots of errors after switching from tslint)
test

# since tsconfig ignores
.eslintrc.js